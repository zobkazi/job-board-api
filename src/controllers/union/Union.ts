import {Request, Response, NextFunction} from "express";


const GetUnions = (req: Request, res: Response, next: NextFunction) => {
    try {
        
    } catch (error) {
        next(error)
    }
}


export default GetUnions



const getUnionsByUpazilaId = (req: Request, res: Response, next: NextFunction) => {
    try {
        
    } catch (error) {
        next(error)
    }
}